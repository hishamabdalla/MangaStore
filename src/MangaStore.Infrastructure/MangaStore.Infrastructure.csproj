<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>MangaStore.Infrastructure</RootNamespace>
    <AssemblyName>MangaStore.Infrastructure</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- ASP.NET Core Identity's password-reset token providers (DataProtectorTokenProvider and
         AddDefaultTokenProviders) ship in the shared framework, not in the standalone
         Microsoft.Extensions.Identity.Core package. Also supplies options DataAnnotations validation. -->
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\MangaStore.Domain\MangaStore.Domain.csproj" />
    <ProjectReference Include="..\MangaStore.Application\MangaStore.Application.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Identity.EntityFrameworkCore" />
    <PackageReference Include="Microsoft.IdentityModel.JsonWebTokens" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.SqlServer" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Sqlite" />
    <!-- Forces the patched SQLitePCLRaw over the 2.1.11 that EF Core Sqlite resolves. See Directory.Packages.props. -->
    <PackageReference Include="SQLitePCLRaw.bundle_e_sqlite3" />
    <PackageReference Include="SQLitePCLRaw.lib.e_sqlite3" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" />
    <PackageReference Include="Microsoft.Extensions.Diagnostics.HealthChecks.EntityFrameworkCore" />
    <PackageReference Include="Scrutor" />
  </ItemGroup>

</Project>
